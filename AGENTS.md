# SkillForge 개발 지침

## 목적과 현재 단계

반품·환불용 모의 업무에서 ReAct 실행 기록을 검증 가능한 선형 워크플로로 전환한다. FastAPI/React 콘솔, 실제 Nemotron/NAT/MCP, 후보 검증·재사용을 구현 중이다. 현재 증거와 미완료 항목은 docs/reviews/implementation-audit.md를 우선 확인한다. 원래 요구사항을 완료 증거 없이 축소하거나 완료로 표시하지 않는다.

## 읽기 순서

README.md → .kiro/specs/skillforge-mvp/requirements.md → design.md → tasks.md. UI는 docs/02-ux-design.md, 평가 변경은 docs/03-evaluation.md도 읽는다. 첨부 원문보다 최신 스펙을 우선한다.

## 범위

- P0는 환불 한 가지 업무, 선형 JSON interpreter, 규칙 기반 정책 검사, 모의 DB, 실제 NVIDIA API 실행 증빙이다.
- 임의 Python/shell 코드 생성·실행, 일반 DAG, PrefixSpan, 모델 학습, 실제 결제는 MVP에서 제외한다.
- 모델이 생성한 문자열을 eval/exec하지 않는다. 도구 allowlist와 구조화된 참조만 사용한다.
- 모든 변경 도구는 동일한 서버 측 정책 검사와 거래 경계를 통과한다. ReAct도 예외가 아니다.
- 실패·거절 기록도 남기되 숨은 추론 전문을 수집하지 않는다. 외부에 노출된 tool call, 결과, 사용량, 상태만 기록한다.
- 합성 시나리오/모의 모델/실제 API/녹화 재생을 구별한다. 벤치마크 수치를 만들지 않는다.

## 작업 방법

- 한 번에 명시된 태스크와 필수 의존 작업을 수행한다. 요구사항 ID와 검증 결과를 보고한다.
- 행동이 바뀌면 요구사항·설계·태스크를 함께 맞춘다. 통상적인 구현 선택마다 승인을 요청하지 않는다.
- Python type hint와 엄격한 입력 모델, TypeScript 타입을 쓴다. 버전은 T00에서 확인한 조합으로 lock한다.
- 핵심 상태 변경, 격리, 컴파일러, 평가에 의미 있는 테스트를 작성한다. 문구·단순 스타일 변경에 형식적 테스트를 추가하지 않는다.
- 사용자 동의 없이 메시지 발송, 신청서 제출, 유료 자원 생성, 외부 배포를 하지 않는다.
- 기존 파일과 사용자 변경을 보존한다. 키를 읽어서 출력하지 않는다.
- 태스크 체크는 구현과 해당 검증이 모두 끝난 경우만 한다. 실패는 원인과 다음 단계로 기록한다.

## 완료 보고

변경 내용 / 대응 REQ·T ID / 수행한 검증과 결과 / 남은 한계. 제품 성능과 계획 목표를 혼동하지 않는다.
